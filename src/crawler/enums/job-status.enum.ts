export enum JobStatus {
  WAITING = 'waiting',
  ACTIVE = 'active',
  DELAYED = 'delayed',
  COMPLETED = 'completed',
  FAILED = 'failed',
  PAUSED = 'paused',
  STALLED = 'stalled',
  BEING_PROCESSED = 'being_processed',
  CANCELED = 'cancelled',
}
